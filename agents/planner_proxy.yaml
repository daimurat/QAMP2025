agent_id: planner_proxy
name: PlannerProxy
type: user_proxy

description: |
  Entry point that receives user input and communicates with PlannerAgent

proxy_config:
  human_input_mode: NEVER
  max_consecutive_auto_reply: 0
  code_execution_config: false

# Made with Bob
